import React, { useState } from "react";

const Page: React.FC = () => {
  return (
    <div className="layout" style={{ display: "flex" }}>
      {/* Insert panels or content */}
    </div>
  );
};

export default Page;
